using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JetpackFlight : MonoBehaviour
{
    private float flightForce; //the amount of force that the jetpack will propel to
    private float flightSpeed;
    public float flightTime; //how much time that the player will fly for
    private bool flying = false; //a boolean to keep track when player is flying
    private bool grounded = true; //a boolean to keep track of when player is on ground

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
